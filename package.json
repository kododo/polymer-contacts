{
  "name": "polymer-contacts",
  "version": "1.0.0",
  "description": "A simple contacts app made with Polymer 2",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dani": "echo 'hola'",
    "watch": "browser-sync start --proxy localhost:8081 --files \"src/**/*.*, index.html, *.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.24.6"
  }
}
